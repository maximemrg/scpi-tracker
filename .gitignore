# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# La BDD "produit" et le classeur SONT versionnés (l'historique est le produit,
# la CI les committe chaque semaine — cf. README). On ignore seulement le cache
# HTTP et le gabarit de saisie local.
/data/*.sqlite-wal
/data/*.sqlite-shm
/.http_cache/
/saisie_manuelle.yaml

# Données personnelles
/portefeuille.yaml

# Secrets
.env
*.json.key
service_account*.json

# Export HTML généré (à héberger sur votre site)
/public/
